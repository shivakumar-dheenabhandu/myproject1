name: CI-CD Production Style Demo Interviews

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AWS_REGION: ap-south-1
  ECR_REGISTRY: 502309405938.dkr.ecr.ap-south-1.amazonaws.com
  ECR_REPOSITORY: test/project1

jobs:

  # 🔹 Stage 1 - Build
  build:
    name: Build Stage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t cicd-demo-app .

  # 🔹 Stage 2 - Unit Test
  test:
    name: Test Stage
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Unit Tests
        run: |
          echo "Running unit tests..."
          # Example:
          # npm install
          # npm test

  # 🔹 Stage 3 - Security Scan
  security-scan:
    name: Security Scan Stage
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Image for Scan
        run: docker build -t cicd-demo-app .

      - name: Run Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: cicd-demo-app:latest
          scanners: vuln
          format: table

  # 🔹 Stage 4 - Push to ECR
  push:
    name: Push to ECR Stage
    runs-on: ubuntu-latest
    needs: security-scan

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region ap-south-1 \
          | docker login --username AWS --password-stdin $ECR_REGISTRY

      - name: Build Docker Image
        run: docker build -t cicd-demo-app .

      - name: Tag Docker Image
        run: docker tag cicd-demo-app:latest $ECR_REGISTRY/$ECR_REPOSITORY:latest

      - name: Push Docker Image
        run: docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

# name: CI-CD Demo weekend community

# on:
#   push:
#     branches: [ "main" ]
  
#   workflow_dispatch:

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout Code
#       uses: actions/checkout@v3

#     - name: Configure AWS Credentials
#       uses: aws-actions/configure-aws-credentials@v2
#       with:
#         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#         aws-region: us-east-1

#     - name: Login to Amazon ECR
#       run: |
#         aws ecr get-login-password --region us-east-1 \
#         | docker login --username AWS --password-stdin 568166345498.dkr.ecr.us-east-1.amazonaws.com

#     - name: Build Docker Image
#       run: docker build -t cicd-demo-app .

#     - name: Tag Docker Image
#       run: docker tag cicd-demo-app:latest 568166345498.dkr.ecr.us-east-1.amazonaws.com/cicd-demo-app:latest

#     - name: Push Docker Image to ECR
#       run: docker push 568166345498.dkr.ecr.us-east-1.amazonaws.com/cicd-demo-app:latest
